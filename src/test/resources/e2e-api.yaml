openapi: 3.0.3

info:
  title: E2E Test API
  version: 1.0.0

paths:

  /e2e/ping:
    get:
      summary: E2E health endpoint
      responses:
        '200':
          description: Successful response
          content:
            text/plain:
              schema:
                type: string